% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_aoi_to_chm.R
\name{make_inference_tiles}
\alias{make_inference_tiles}
\title{Découper un raster en tuiles pour l'inférence}
\usage{
make_inference_tiles(r, tile_size = 1000, overlap = 200)
}
\arguments{
\item{r}{SpatRaster (déjà à 1.5m)}

\item{tile_size}{Taille des tuiles en mètres}

\item{overlap}{Chevauchement en mètres}
}
\value{
Liste nommée de SpatRasters, portant les attributs \code{margin_x} et
\code{margin_y} (largeur du fondu, en pixels)
}
\description{
Toutes les tuiles ont \strong{exactement les mêmes dimensions en pixels} : la
dernière colonne / ligne est décalée vers l'intérieur au lieu d'être rognée.
Deux raisons, à l'origine de l'effet de bord observé sur la mosaïque :
\itemize{
\item le modèle PVTv2 est reconstruit avec un \code{img_size} déduit de la taille
de la tuile ; des tuiles de tailles différentes donnaient des géométries
de tête de segmentation différentes, donc des prédictions incohérentes
entre le cœur et la bordure de l'AOI ;
\item les tuiles rognées étaient complétées côté Python par des \strong{zéros},
c'est-à-dire une bande noire artificielle que le modèle voit comme du
sol nu.
}
}
\details{
Le chevauchement (200 m par défaut, ~133 px à 1.5 m) alimente le fondu
pondéré de \code{\link[=mosaiquer_predictions]{mosaiquer_predictions()}}.
}
\keyword{internal}

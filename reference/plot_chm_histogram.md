# Histogramme des hauteurs de canopée

Histogramme des hauteurs de canopée

## Usage

``` r
plot_chm_histogram(
  chm_raster,
  title = "Distribution des hauteurs de canopée",
  n_breaks = 50,
  max_cells = 1e+06
)
```

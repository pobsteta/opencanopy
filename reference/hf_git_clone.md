# Cloner le dataset complet via git (nécessite git-lfs)

Cloner le dataset complet via git (nécessite git-lfs)

## Usage

``` r
hf_git_clone(
  dest_dir = file.path(DATA_DIR_HF, "Open-Canopy"),
  token = HF_TOKEN
)
```
